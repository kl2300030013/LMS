NODE_ENV=production
PORT=3001

# Frontend URL (service name inside Docker)
FRONTEND_URL=http://frontend

# Database configuration (use service name `db`)
MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_USER=eduflow
MYSQL_PASSWORD=eduflow
MYSQL_DATABASE=eduflow

# Security
JWT_SECRET=dev_secret
JWT_EXPIRE=7d

# Remove if not using Supabase
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_key
